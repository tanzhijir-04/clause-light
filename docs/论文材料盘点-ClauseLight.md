# ClauseLight 论文材料盘点

更新时间：2026-08-25

用户已确认：当前 2 份 PDF 和 TXT 合同样本可以合法使用，并允许在完成脱敏后用于论文案例整理。

交付口径：论文初稿以 Markdown 维护，便于快速修改和补材料；最终投稿版本使用 TeX 渲染为 PDF，不以 Word 文档作为最终交付格式。

## 一、目前已经找到的材料

### 1. 系统设计与实现证据

| 材料 | 路径 | 可支持的论文内容 | 状态 |
|---|---|---|---|
| Agent 主流程 | `server/core/agent.py` | 文档解析、Stage 1、五维并行、冲突复核、Stage 3 聚合 | 可直接使用 |
| 条款解析 | `server/core/workers/parser.py` | 合同类型、条款结构和风险维度路由 | 可直接使用 |
| 风险 Worker | `server/core/workers/workers.py` | 五类风险维度、统一输出字段、冲突检测 | 可直接使用，需说明当前边界 |
| 结果聚合 | `server/core/workers/evaluator.py` | 红黄绿统计、综合分数、建议和人工复核标记 | 可直接使用，需补评分规则说明 |
| 文档入口 | `server/core/document_ingress.py` | PDF、图片、OCR 和降级路径 | 可直接使用 |
| 知识库 | `server/core/knowledge.py` | 规则和法规检索、反馈学习接口 | 可写入系统设计，效果暂不能宣称 |
| 架构文档 | `docs/AGENT_ARCHITECTURE.md` | 总体流程、阶段划分、调用预算和降级策略 | 可直接使用 |
| 项目说明 | `README.md` | 功能范围、技术栈、部署和页面 | 可作为辅助证据 |

### 2. 测试和交付证据

当前项目目录中发现：

- 40 个 Python 测试文件；
- 约 339 个测试定义；
- OCR 测试报告：历史记录为 27 个测试通过；
- WebSocket 测试报告：历史记录为 23 个测试通过；
- 合同 API 测试报告：历史记录为 11 个测试通过；
- 知识库测试报告：历史记录为 27 个测试通过；
- 历史覆盖率报告：176 个测试，175 个通过，1 个失败，整体覆盖率 68%；
- 当前全量 pytest：253 个通过、2 个跳过、23 个警告；测试收集和旧接口路径问题已完成同步修复，2 个跳过项为未安装 Outlines 时的可选依赖测试。

这些测试主要证明接口、数据结构、错误处理和模块行为，不等于合同风险识别准确率。

本轮已补充可核对的测试环境信息：Microsoft Windows 11 专业工作站版 64 位，AMD Ryzen 7 5700X3D 8 核 8 线程，Python 3.12.5，Git 短提交号 `3e6261b5`，FastAPI 0.110.2，SQLAlchemy 2.0.49，Pydantic 2.13.4，OpenAI Python SDK 2.45.0，PyMuPDF 1.27.2.3，PaddleOCR 3.7.0，PaddlePaddle 3.3.1，Pillow 12.1.0，pytest 9.0.3，pytest-asyncio 1.4.0，pytest-cov 7.1.0。当前环境未安装 `outlines` 和 `sentence-transformers`。

### 3. 截图和前端功能证据

现有截图目录：

- `tests/screenshots/`：6 张基础页面截图；
- `tests/screenshots/full/`：仪表盘、合同列表、合同详情、原文标注、条款列表、上传、知识库、同步、模型和设置等页面截图；
- `tests/screenshots/quick/`：快速 E2E 测试页面截图。

可用于论文中的“系统界面”和“运行流程”插图，但截图中的合同内容需要确认是否已经脱敏。

### 4. 本地运行数据

对 `data/clause_light.db` 进行只读统计得到：

| 数据表 | 记录数 | 论文用途 |
|---|---:|---|
| contracts | 10 | 系统曾接收的合同记录规模 |
| analyses | 7 | 已保存的分析记录规模 |
| clause_analyses | 92 | 已保存的条款分析记录规模 |
| knowledge_rules | 48 | 本地知识规则规模 |
| legal_references | 43 | 法规资料规模 |
| memory_atoms | 17 | Agent 记忆运行痕迹 |
| memory_events | 9 | Agent 事件记录 |
| memory_sessions | 3 | 记忆会话记录 |
| wiki_pages | 6 | Wiki 资料记录 |

7 条分析记录的综合分数为 18、25、25、30、35、41、41，平均值约为 30.7；关联条款风险分布为红色 45、黄色 29、绿色 18。

这些数字只能作为“本地运行记录统计”，不能作为模型准确率、召回率或系统可靠性指标。不同记录可能来自重复文件或同一合同的多次运行。

### 5. 合同文件材料

`data/uploads/` 中共有 44 个 PDF 文件名，但存在大量重复和无效文件。按文件哈希和 PDF 解析结果检查：

- 约 29 个文件是同一个 16 字节的截断/无效 PDF；
- 12 个文件是同一份约 74 KB、2 页、约 1425 个文字字符的 PDF；
- 4 个文件是同一份约 5 MB、2 页、约 1550 个文字字符的 PDF；
- 另有一个约 4233 字节的 TXT 合同样本；
- 另有两个 13 字节的 DOCX 测试占位文件。

因此，目前只能确认约 2 份内容不同且可以正常打开的 PDF，不能把上传目录写成 44 份合同数据集。

## 二、可以直接写进论文的内容

### 第三章：系统总体设计

可以使用现有代码和架构文档完整描述：

- 文档解析与 OCR 降级；
- Stage 1 条款结构化；
- Stage 2 五维风险 Worker 并行；
- 冲突检测和二次分析；
- Stage 3 结果聚合；
- SQLite 本地存储和模型统一网关。

### 第四章：系统实现

可以使用实际源码说明：

- Python + FastAPI 后端；
- SQLAlchemy + SQLite 数据层；
- OCR、Embedding、LLM 和知识库接口；
- 前端合同列表、详情、原文标注和知识库页面；
- API、WebSocket、重试和异常处理。

### 第五章：功能验证

可以写：

- 已有单元测试、API 测试和 E2E 测试体系；
- 已有界面截图和本地运行数据库；
- 历史测试报告记录了 OCR、WebSocket、合同 API 和知识库模块的测试结果；
- 系统确实完成过合同上传、解析、分析和结果保存流程。

## 三、当前不能直接写成论文结论的内容

- 不能写“准确率达到某个百分比”：没有人工金标准；
- 不能写“系统显著优于普通 LLM”：没有对照实验；
- 不能写“支持 44 份合同数据集”：上传目录高度重复且包含无效文件；
- 不能写“系统稳定运行”：日志中曾出现大量知识库接口 500 错误；
- 不能写“知识库自动学习有效”：数据库有自动学习记录，但没有正确率或人工确认实验；
- 不能写“法律判断准确”：没有法律专业人员标注和复核；
- 不宜把测试通过数直接表述为合同风险识别准确率；当前全量测试无失败，但有 2 个可选依赖测试跳过和 23 个警告，需要在投稿前逐项归类。

## 四、已确认的技术问题

### 1. Prompt 测试引用缺失模块（已处理）

`tests/test_prompts.py` 引用了以下模块，但当前 `server/core/prompts/` 目录中未找到对应文件：

- `server.core.prompts.analyze`；
- `server.core.prompts.classify`；
- `server.core.prompts.score`；
- `server.core.prompts.split`；
- `server.core.prompts.suggest`。

该问题源于测试仍引用已被当前架构移除的旧 Prompt 模块。现已将测试同步到当前 Worker 和 Distill Prompt 构造函数，全量 pytest 可以正常收集并执行。

### 2. `/api/devices` 历史失败（已处理）

历史失败用例 `tests/test_main.py::TestDevicesEndpoint::test_list_devices` 请求了旧路径 `/api/devices`，而当前路由为 `/api/connection/devices`。现已同步测试路径并按当前返回结构校验，全量测试不再因该问题失败。

### 3. 历史数据库迁移不一致

旧运行日志中出现 `knowledge_rules.status` 字段不存在导致的数据库错误，并伴随大量知识库相关 500 响应。当前数据库表结构已包含 `status` 字段，但历史日志仍然反映过迁移不一致问题。论文中应把它作为开发过程中的问题或局限，不应把历史运行直接概括为稳定运行。

### 4. 外部模型和 Embedding 兼容问题

运行日志记录过：

- 外部模型不支持当前 `response_format` 类型，Outlines 结构化输出失败后回退 JSON 解析；
- Embedding 初始化曾因 `SentenceTransformer` 参数兼容问题失败；
- 部分流程因此依赖降级路径。

这些内容可写入“异常处理和系统局限”，不能被隐去。

## 五、真正还需要补充的材料

### A. 论文最低可用材料

- [x] 确认 2 份可打开 PDF 和 TXT 样本拥有合法使用权；
- [x] 明确论文初稿使用 Markdown，最终版使用 TeX 渲染；
- [ ] 对案例截图、合同文件和数据库记录进行脱敏确认；
- [x] 修复 Prompt 测试收集错误；
- [x] 将旧 `/api/devices` 测试同步到当前接口路径；
- [x] 重新运行与论文相关的测试并保存真实输出；
- [x] 记录测试机器、Python、OCR 和代码版本；
- [ ] 选取 1—3 个案例，整理输入、条款、风险结果和人工复核说明；
- [ ] 确认代码中的风险阈值来源或将其改写为系统经验规则；
- [ ] 将“分析失败”状态从绿色占位改为未知/待复核，避免安全含义混淆。

### B. 如果希望论文更像科研论文

- [ ] 准备更多合法脱敏合同；
- [ ] 制定条款和风险等级标注表；
- [ ] 至少由 1 名具备法律背景的人员进行复核；
- [ ] 设置单轮 LLM、串行分析和多阶段 Agent 等对照方案；
- [ ] 记录条款识别、风险等级一致性、运行时间和调用成本；
- [ ] 对并行分析、冲突复核和知识库检索做消融比较。

## 六、建议写入论文的材料边界

当前最稳妥的表述是：

> 本文通过功能测试、系统运行记录和典型案例，验证 ClauseLight 的合同上传、文档解析、条款分析、风险展示和结果持久化流程，重点讨论系统设计与工程实现，不将有限案例结果解释为法律风险识别准确率。

这句话与目前实际证据相符，也不会把运行数据库误写成正式数据集。
